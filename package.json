{
    "name": "mi-proyecto",
    "version": "1.0.0",
    "scripts": {
        "start": "npm install concurrently --save-dev && concurrently \"npm --prefix backend install && npm --prefix backend start\" \"npm --prefix frontend install && npm --prefix frontend run dev\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
